# Fixture

The taxonomy is decided in [ADR-0001](adr/0001-alpha.md), which is here.
The rationale is in [the design note](docs/design.md), which is not.
The worklist is [LEDGER.md](LEDGER.md) — a root-level path, matched exactly rather
than by directory prefix, and absent from this fixture.
External links are not a file check: [the spec](https://example.com/spec.md), and
neither are anchors: [above](#fixture).
