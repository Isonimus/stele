# Conventions

Slices are frozen at merge (ADR-0001). Routing is settled by ADR-0042, which is a
decision this corpus does not contain — read at the top of every session regardless.
A decision in another repo stays skipped (boxel:ADR-0134).
